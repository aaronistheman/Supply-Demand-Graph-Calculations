"use strict";

QUnit.module("tax on demand");

  /* Probably needs to be recalculated
  QUnit.test("correct consumer surplus adjustment",
    function(assert) {
    var data = getLinearGraph1();
    
    data.setTax(Graph.Demand, 0.3);
    assert.deepEqual(data.getConsumerSurplus().get(),
      400); // hand-calculated
  });
  */