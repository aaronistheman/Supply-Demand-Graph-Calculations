"use strict";

QUnit.module("price ceiling, tax, and subsidy");

  function testCeilingTaxSubsidySetUp(setupNum) {
    switch (setupNum) {
    case 1:
      var data = getLinearGraph1();
      data.
      return data;
    default:
      alertAndThrowException("Invalid setupNum");
    }
  }